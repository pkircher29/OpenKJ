name: Disabled legacy upstream container publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in the community fork; it targeted the upstream GHCR namespace."
