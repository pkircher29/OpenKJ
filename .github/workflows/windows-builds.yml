name: Disabled legacy upstream Windows release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in the community fork; use build-3.0-matrix.yml."
